"""Константы Pac-Man: размеры сетки, тайминги, цвета, очки.

Экран специально **крупный**: тайл 28 px (в плане был 24). Всё остальное
считается от `TILE`, поэтому размер игры меняется одной строкой.
"""

# --- Сетка и экран ---------------------------------------------------------
COLS = 28                      # ширина лабиринта в клетках (классический Pac-Man)
ROWS = 31                      # высота лабиринта в клетках
TILE = 28                      # размер клетки в пикселях (крупнее обычного)

FIELD_W = COLS * TILE          # 784 — ширина игрового поля
FIELD_H = ROWS * TILE          # 868 — высота игрового поля

HUD_TOP = 2 * TILE             # верхняя панель: счёт / рекорд
HUD_BOTTOM = 2 * TILE          # нижняя панель: жизни / уровень-фрукты

WIDTH = FIELD_W                # 784
HEIGHT = HUD_TOP + FIELD_H + HUD_BOTTOM   # 980
FPS = 60

TUNNEL_ROW = 14                # ряд бокового тоннеля (обёртка слева↔справа)

# --- Направления (dx, dy) --------------------------------------------------
NONE = (0, 0)
UP = (0, -1)
DOWN = (0, 1)
LEFT = (-1, 0)
RIGHT = (1, 0)

# --- Скорости (пикс/кадр); делители TILE, чтобы держать привязку к сетке ----
PACMAN_SPEED = 2               # 28 / 2 = 14 шагов на клетку

# --- Очки ------------------------------------------------------------------
PTS_DOT = 10
PTS_ENERGIZER = 50
EXTRA_LIFE_AT = 10000
START_LIVES = 3

# --- Тайминги (мс) ---------------------------------------------------------
READY_MS = 2000                # пауза «READY!» перед стартом раунда
LEVEL_CLEAR_MS = 1600          # мигание лабиринта после зачистки

# --- Цвета -----------------------------------------------------------------
BLACK = (0, 0, 0)
WALL = (33, 33, 222)           # классический синий лабиринт
WALL_EDGE = (80, 80, 255)
GATE = (255, 183, 174)         # воротца дома призраков
DOT = (255, 184, 151)          # точки-«персик»
ENERGIZER = (255, 184, 151)
PACMAN = (255, 255, 0)
TEXT = (255, 255, 255)
READY_COLOR = (255, 255, 0)
