"""Настройки игры Battle City и цветовая палитра."""

# --- Поле ---
TILE = 40                      # размер одной клетки (кирпич/сталь) в пикселях
COLS = 13                      # ширина поля в клетках
ROWS = 13                      # высота поля в клетках
FIELD_W = COLS * TILE          # 520
FIELD_H = ROWS * TILE          # 520
HUD_W = 150                    # ширина боковой панели справа
WIDTH = FIELD_W + HUD_W
HEIGHT = FIELD_H
FPS = 60

# --- Танки ---
TANK_SIZE = 32                 # базовый габарит танка (меньше клетки — проходит в проёмы)
TANK_OFFSET = (TILE - TANK_SIZE) // 2   # центрирование танка в полосе
# Габарит танка игрока по уровню звезды (0..3): чем больше звёзд, тем крупнее.
# Все размеры < TILE, чтобы танк по-прежнему проходил в одноклеточные проёмы.
PLAYER_TANK_SIZES = (32, 34, 36, 38)
PLAYER_SPEED = 2               # пикселей за кадр
ENEMY_SPEED = 2
PLAYER_LIVES = 3
PLAYER_MAX_LIVES = 9           # потолок жизней (бонус «орёл» не копит сверх)
PLAYER_INVULN_MS = 2000        # неуязвимость после респауна

# --- Пули ---
BULLET_SPEED = 6
BULLET_SIZE = 8
PLAYER_SHOOT_COOLDOWN = 350    # мс между выстрелами игрока
PLAYER_MAX_BULLETS = 1         # сколько пуль игрока одновременно на поле

# --- Враги ---
ENEMY_COUNT_MIN = 10           # минимум врагов за уровень (рандом)
ENEMY_COUNT_MAX = 15           # максимум врагов за уровень (рандом)
MAX_ACTIVE_ENEMIES = 4         # одновременно на поле
ENEMY_START_DELAY = 6000       # пауза перед первым врагом (мс), ~6 сек
ENEMY_SPAWN_INTERVAL = 2500    # мс между появлениями
ENEMY_SHOOT_CHANCE = 0.012     # вероятность выстрела за кадр
ENEMY_TURN_CHANCE = 0.02       # вероятность смены направления за кадр
ENEMY_SCORE = 100              # очков за уничтоженного врага
BONUS_ENEMY_CHANCE = 0.25      # доля врагов-носителей бонуса (мигают)

# --- Типы врагов (как в оригинале Tank 1990) ---
#   basic — обычный: средняя скорость, 1 HP
#   fast  — быстрый: юркий, 1 HP, давит манёвром
#   power — скорострельный: быстрые пули, 1 HP
#   armor — броневой: 4 попадания, меняет цвет по мере пробития
ENEMY_TYPES = {
    "basic": dict(speed=2, hp=1, fast_bullet=False, size=32, score=100,
                  color=(208, 212, 216)),
    "fast":  dict(speed=3, hp=1, fast_bullet=False, size=30, score=200,
                  color=(140, 205, 225)),
    "power": dict(speed=2, hp=1, fast_bullet=True,  size=32, score=300,
                  color=(232, 196, 116)),
    "armor": dict(speed=2, hp=4, fast_bullet=False, size=36, score=400,
                  color=(180, 160, 180)),
}
# Состав врагов по уровням: (мин. уровень 1-based, {тип: вес}). Берётся последняя
# запись, чей порог ≤ текущего уровня; тип выбирается взвешенным рандомом.
ENEMY_SPAWN_TABLE = [
    (1,  {"basic": 8, "fast": 2}),
    (4,  {"basic": 6, "fast": 3, "power": 2}),
    (7,  {"basic": 4, "fast": 3, "power": 3, "armor": 2}),
    (10, {"basic": 3, "fast": 3, "power": 3, "armor": 4}),
    (15, {"fast": 3, "power": 4, "armor": 5}),
]

# --- Появление врага: анимация «портала» ---
SPAWN_ANIM_MS = 900            # сколько мигает звезда-портал перед появлением врага
SPAWN_COLOR_A = (255, 255, 255)
SPAWN_COLOR_B = (120, 210, 240)

# --- Тряска экрана и вспышка (juice) ---
SHAKE_SMALL = 3                # амплитуда тряски (px) при гибели танка
SHAKE_BIG = 7                  # при разрушении базы / бомбе
SHAKE_MS = 300                 # длительность затухающей тряски
FLASH_MS = 140                 # длительность белой вспышки

# --- Усиление врагов после 10-го уровня ---
TOUGH_ENEMY_FROM_LEVEL = 11    # с какого уровня (1-based) враги «усиленные»
REINFORCE_SIZE_BONUS = 4       # прибавка к габариту усиленного врага
REINFORCE_MAX_SIZE = 38        # но не крупнее — иначе застрянет в проёмах
REINFORCE_MIN_HP = 2           # минимум попаданий по усиленному врагу

# Цвета брони по остатку HP (интерполяция «цел → пробит»)
ARMOR_FULL_COLOR = (150, 158, 175)
ARMOR_LOW_COLOR = (214, 96, 80)

# --- Бонусы (power-ups) ---
#   star  — апгрейд танка (крупнее + быстрее/2 пули/пробивает всё)
#   clock — «часы»: замораживает всех врагов на время
#   bomb  — «бомбочка»: мгновенно взрывает всех врагов на поле
#   steel — «сталь»: одевает базу в стальную броню
#   life  — «орёл»: +1 жизнь
POWERUP_KINDS = ("star", "clock", "bomb", "steel", "life")
POWERUP_LIFETIME = 12000       # сколько висит на поле, если не подобрали (мс)
POWERUP_BLINK_MS = 350         # период мигания иконки на поле
POWERUP_SCORE = 200            # очков за подбор
PLAYER_MAX_LEVEL = 3           # уровней апгрейда танка от звезды (0..3)
FREEZE_DURATION = 6000         # заморозка врагов от «часов» (мс)
STEEL_DURATION = 12000         # стальная броня базы от «стали» (мс)

# --- Звук ---
SOUND_ENABLED = True

# --- Направления (dx, dy) ---
UP = (0, -1)
DOWN = (0, 1)
LEFT = (-1, 0)
RIGHT = (1, 0)

# --- Цвета (R, G, B) ---
BG_COLOR = (20, 20, 20)
FIELD_COLOR = (8, 8, 8)
GRID_LINE = (26, 26, 26)        # еле заметная сетка полос
FIELD_BORDER = (70, 70, 70)
HUD_BG = (99, 99, 99)
HUD_TEXT = (28, 28, 28)
WHITE = (236, 236, 236)

BRICK_COLOR = (178, 92, 42)
BRICK_DARK = (92, 44, 22)       # «раствор» между кирпичами
STEEL_COLOR = (190, 195, 205)
STEEL_DARK = (95, 100, 110)

TANK_OUTLINE = (18, 18, 18)
PLAYER_COLOR = (180, 214, 92)   # игрок — зелёный
PLAYER_TRACK = (120, 150, 60)
ENEMY_COLOR = (208, 212, 216)   # враг — серебристый
ENEMY_TRACK = (130, 134, 138)
ENEMY_CORE = (200, 70, 60)      # красный «глаз» врага

BULLET_COLOR = (245, 240, 220)

# Взрыв: от яркой вспышки к огню и дыму
EXPLOSION_FLASH = (255, 248, 220)
EXPLOSION_CORE = (255, 220, 90)
EXPLOSION_MID = (245, 150, 50)
EXPLOSION_OUTER = (210, 70, 40)
EXPLOSION_SMOKE = (110, 92, 80)

BASE_COLOR = (224, 196, 64)
BASE_DARK = (90, 78, 28)
BASE_GROUND = (60, 60, 60)

# Бонусы: рамка-подложка и иконки
POWERUP_BG = (38, 40, 48)
POWERUP_FRAME = (236, 236, 236)
STAR_COLOR = (245, 210, 70)
CLOCK_COLOR = (150, 205, 235)   # «часы» — морозный голубой
BOMB_COLOR = (96, 100, 104)     # «бомбочка» — тёмный металл
STEEL_ITEM_COLOR = (200, 205, 215)  # «сталь» — светлая плита
LIFE_COLOR = (180, 214, 92)     # «орёл» (+1 жизнь) — зелёный, как игрок
SHIELD_COLOR = (120, 210, 240)
FREEZE_TINT = (120, 200, 240)   # оттенок замороженного врага

TEXT_COLOR = (236, 236, 236)
ACCENT = (220, 80, 60)
